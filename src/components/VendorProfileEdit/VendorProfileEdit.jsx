// View for logged-in vendor to edit their details
// Reached by path '/edit/${vendorUserId}'   --can we use same link for Planner Profile edit but conditionally render based on user type?
import React from 'react';


function VendorProfileEdit({ theme }) {
  return (
    <div className="container">
      <p>Vendor Profile Edit</p>
    </div>
  );
}

export default VendorProfileEdit;